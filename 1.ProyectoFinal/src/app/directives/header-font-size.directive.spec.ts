import { HeaderFontSizeDirective } from './header-font-size.directive';

describe('HeaderFontSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new HeaderFontSizeDirective();
    expect(directive).toBeTruthy();
  });
});
